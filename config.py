DEFAULT_CONFIG = {
    # 新增目录限制配置
    "directory_restrictions": {
        "allowed_paths": ["./workspace"],  # 默认可操作路径
        "allow_outside_workspace": False,  # 是否允许操作workspace外的文件
    },
    # ... 其他现有配置 ...
}
